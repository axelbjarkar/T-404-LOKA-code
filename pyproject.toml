[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "loka"
version = "0.1.0"
dependencies = [
    "torch",
    "numpy",
    "scikit-learn",
    "tqdm",
]

[tool.setuptools]
packages = ["loka"]
